// Mise entry point. Real app arrives at Task 2 (hello-world PWA);
// this placeholder keeps jsconfig's include non-empty so tsc runs.

/** @type {{ name: string, version: string }} */
export const APP = { name: "Mise", version: "0.1.0" };
